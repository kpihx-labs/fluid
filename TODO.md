# TODO

- Add a real project example repo using `Dockerfile`, `docker-compose.yml`, `fluid.yml`, and GitLab CI.
- Add storage guidance for stateful stacks such as Nextcloud and Postgres.
- Add a dedicated worker profile for a future Linux node hosted on Proxmox.

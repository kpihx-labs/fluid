# --- Fluid-K3s: Agnostic Cluster Configuration (Universal Registry) ---
# PURPOSE: This file is the single source of truth for the entire federation.
# WHY: Transition to a 100% DECENTRALIZED architecture using CockroachDB Mesh.
# -----------------------------------------------------------------------------

# 📂 Inventory Configuration
# -----------------------------------------------------------------------------
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
: ${INVENTORY_PATH:="$SCRIPT_DIR/hosts.json"}

# 🛠️ Inventory Helper Functions
get_node_val() {
    local node_name=$1
    local key=$2
    jq -r ".nodes[] | select(.name == \"$node_name\") | .$key" "$INVENTORY_PATH" 2>/dev/null
}

# WHY: In a mesh, we need a list of all peer IPs to join the cluster.
get_db_join_list() {
    # Extract IPs of PVE and Ubuntu as the primary DB backbone.
    jq -r '.nodes[] | select(.name == "pve" or .name == "ubuntu") | .tailscale_ip' "$INVENTORY_PATH" | paste -sd "," -
}

# 🚀 Remote Execution & Privileged Access
# -----------------------------------------------------------------------------
: ${REMOTE_CMD:="ssh"}
: ${SUDO_CMD:="sudo"}
: ${REMOTE_SUDO:="sudo"}

# 🌐 Network & Failover Configuration
# -----------------------------------------------------------------------------
: ${CLUSTER_DOMAIN:="fluid.local"}
: ${CLUSTER_VIP:="10.20.20.250"}
: ${FLUID_BRIDGE:="br-fluid"}
: ${VXLAN_ID:="42"}
: ${VXLAN_PORT:="10442"}
: ${VXLAN_UNDERLAY_INTERFACE:="tailscale0"}
: ${VXLAN_MTU:="1150"}
: ${VXLAN_SUBNET:="10.20.20.0/24"}
: ${CLUSTER_INTERFACE:="vxlan-fluid"}

# 🧠 Decentralized Datastore (CockroachDB Mesh)
# -----------------------------------------------------------------------------
# WHY: We replace single Postgres with CockroachDB to achieve 100% HA.
: ${DB_TYPE:="cockroach"}
: ${DB_USER:="root"}
: ${DB_NAME:="k3s"}
: ${DB_PORT:="26257"} # WHY: Standard CockroachDB port.
: ${DB_HTTP_PORT:="8080"} # WHY: Console UI port.

# WHY: Every node points to its LOCAL CockroachDB instance.
# The synchronization is handled in the background by the DB mesh.
: ${DB_HOST:="127.0.0.1"}

# 🏗️ Proxmox (PVE) Provisioning Defaults
# -----------------------------------------------------------------------------
: ${PVE_HOST:="pve"}
: ${PVE_STORAGE:="local-lvm"}
: ${VM_ID_START:="100"}
: ${VM_NAME_PREFIX:="fluid-k3s-node"}
: ${VM_MEMORY:="4096"}
: ${VM_CORES:="2"}
: ${VM_DISK_SIZE:="20"}

# 🛰️ Current Node Context Detection
# -----------------------------------------------------------------------------
CURRENT_HOSTNAME=$(hostname)
NODE_MATCH=$(jq -r ".nodes[] | select(\"$CURRENT_HOSTNAME\" | contains(.name)) | .name" "$INVENTORY_PATH" | head -n 1)

if [ -n "$NODE_MATCH" ] && [ "$NODE_MATCH" != "null" ]; then
    export NODE_NAME="$NODE_MATCH"
    export NODE_OS=$(get_node_val "$NODE_NAME" "os")
    export NODE_IP=$(get_node_val "$NODE_NAME" "tailscale_ip")
    export NODE_VXLAN_IP=$(get_node_val "$NODE_NAME" "vxlan_ip")
    echo "[CONFIG] Node Detected: $NODE_NAME (OS: $NODE_OS, IP: $NODE_IP)"
else
    export NODE_NAME="$CURRENT_HOSTNAME"
    export NODE_OS="linux"
    echo "[WARNING] Node '$CURRENT_HOSTNAME' not in inventory."
fi

# 📁 Storage & Versions
# -----------------------------------------------------------------------------
: ${LONGHORN_REPLICAS:="2"}
: ${K3S_VERSION:="v1.33.10+k3s1"}
: ${COCKROACH_VERSION:="v23.2.3"} # WHY: Stable distributed SQL version.
: ${LONGHORN_VERSION:="v1.6.1"}
: ${ESO_VERSION:="v2.4.0"}
: ${K3S_TOKEN:="fluid_sovereign_secret_2026"}

# Systemd Templates

This directory is intentionally generic.

Generated systemd units belong under `runtime/rendered/`.
If a platform or host needs different behavior, that change must come from
configuration, not from hardcoded host-specific templates here.

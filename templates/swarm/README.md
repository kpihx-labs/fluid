# Swarm Templates

This directory stores generic Swarm-oriented assets.

Current Fluid behavior keeps most project placement logic in repo-local
`fluid.yml` files and generates overrides into `runtime/rendered/projects/`.
This template directory exists for reusable cluster-wide assets when Fluid
needs them later.
